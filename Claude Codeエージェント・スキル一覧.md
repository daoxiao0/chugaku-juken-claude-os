# Claude Code エージェント & スキル 一覧

このファイルは、本プロジェクトで使える Claude Code の「エージェント（Agent）」と「スキル（Skill）」を整理したものです。中学受験プロジェクトそのものの内容ではなく、**作業を進めるためのツール群の説明**です。

---

## 1. 全体像：エージェント と スキル の違い

| 概念 | 何か | 呼び方 |
|------|------|--------|
| **エージェント（Agent）** | 別の「作業員」を一時的に立ち上げて、調査・分析・実装などを任せる仕組み。呼び出した側（今の私）とは別のコンテキスト（記憶）で動く | `Agent` ツールで起動 |
| **スキル（Skill）** | あらかじめ用意された「専門知識・手順テンプレート」。今の会話の中でそのまま実行される（別人格を立ち上げるわけではない） | `/コマンド名` または `Skill` ツールで起動 |

- **スキル**＝このプロジェクト用に作られた「型」（保護者戦略室、志望校戦略室など）。**普段使うのはほぼこちら。**
- **エージェント**＝汎用の調査・実装用の「作業員」。中学受験プロジェクトでは基本的に使わないが、もし将来「Web上の中学入試情報を大量に調べる」等の重い調査が必要になった場合に使う可能性がある。

---

## 2. 使えるエージェント（Agent）一覧

| エージェント名 | できること | 使えるツール |
|---------------|-----------|-------------|
| `claude` | 特定の型に当てはまらない何でも屋。指定なしのデフォルト | 全ツール |
| `claude-code-guide` | Claude Code自体の使い方（機能・hooks・スラッシュコマンド・設定）に関する質問に回答 | 検索・Web系のみ |
| `Explore` | コード/ファイルの高速検索専用。「このキーワードはどこにある？」に強い。分析や比較には不向き | 読み取り系のみ（編集不可） |
| `general-purpose` | 複雑な調査・複数手順のタスクを一括で任せる汎用エージェント | 全ツール |
| `Plan` | 実装方針・設計を計画するアーキテクト役。手順書やトレードオフの提示に強い | 読み取り系（編集不可） |
| `statusline-setup` | Claude Codeのステータスバー表示設定専用 | 読み書き最小限 |

**このプロジェクトでの使いどころ（想定）**
- 現状、本プロジェクトの成果物作成（週次戦略、志望校分析など）は**スキルで十分**カバーされている
- もし「複数の学校の公式サイトを一気に調べて比較資料を作る」など**大量・並行の調査**が必要になったら `general-purpose` や `Explore` を使う価値がある
- エージェントは**明示的に指示された場合のみ**起動する運用ルールになっている（勝手に増殖させない）

---

## 3. 使えるスキル（Skill）一覧

### 3-1. 本プロジェクト専用スキル（`.claude/commands/` で定義）

| スキル | コマンド | 役割 |
|--------|---------|------|
| **parent-strategy** | `/parent-strategy` | 保護者戦略室を起動。今週の状況を入力すると、親子の週次行動計画を出力する司令塔 |
| **school-strategy** | `/school-strategy` | 志望校戦略室を起動。学校の分析・比較・出願戦略を立案。分析結果は自動で `志望校戦略/成果物/` にMD保存される |
| **habit-coach** | `/habit-coach` | 学習習慣コーチを起動。子どもが自ら机に向かうための習慣化プラン・親の関わり方を提案 |

→ AGENTS.md記載の「フォルダ構成」と対応：保護者戦略室・志望校戦略・学習習慣コーチの3つは既にスキル化済み。**模試分析・過去問分析・メンタルサポート・受験スケジュール管理はまだスキル化されていない**（フォルダのAGENTS.mdはあるが、専用スラッシュコマンドはまだ無い）。

### 3-2. Claude Code 汎用スキル

| スキル | 用途 |
|--------|------|
| `update-config` | settings.json の設定変更（権限追加、hooks設定、環境変数など） |
| `keybindings-help` | キーボードショートカットのカスタマイズ |
| `verify` | 実装した変更が実際に動くか、アプリを動かして確認 |
| `code-review` | 現在の diff をレビュー（バグ・簡潔化の指摘） |
| `simplify` | 変更箇所のコードを簡潔化 |
| `fewer-permission-prompts` | よく使うコマンドを許可リスト化し、確認プロンプトを減らす |
| `loop` | プロンプト/スラッシュコマンドを一定間隔で繰り返し実行 |
| `schedule` | クラウド上で定期実行するエージェント（cronジョブ）の作成・管理 |
| `claude-api` | Claude API/Anthropic SDKの仕様リファレンス |
| `run` | アプリを起動して動作確認 |
| `init` | 新規CLAUDE.mdの初期化 |
| `review` | GitHub PRのレビュー |
| `security-review` | 変更差分のセキュリティレビュー |

※これらはソフトウェア開発向けのスキルで、**中学受験プロジェクトでは通常使わない**（このプロジェクトはコードを書くプロジェクトではないため）。

---

## 4. どう連携させるか

- **エージェント同士は自動で連携しない。** 私（メインの会話）が必要と判断した時だけ `Agent` ツールで個別に起動し、結果を受け取って統合する。エージェント間の直接会話はしない（`SendMessage` で同じエージェントに継続依頼はできる）。
- **スキルはその場で私が直接実行する**もので、別人格を立てるわけではないため、会話の文脈（これまでの模試結果・方針など）をそのまま引き継げる。**日常的な運用ではスキルの方が自然。**
- 本プロジェクトの基本フローは AGENTS.md 記載どおり：
  1. 迷ったら `/parent-strategy`（司令塔）
  2. 志望校を絞るなら `/school-strategy`
  3. やる気の問題なら `/habit-coach`
  4. これら以外の専門分野（模試分析・過去問分析・メンタルサポート・スケジュール管理）は今のところ**フォルダのAGENTS.mdを読んで手動で対応**する運用（スキル化されていないため）

---

## 5. 今後の検討ポイント（提案）

- 模試分析・過去問分析・メンタルサポート・受験スケジュール管理の4フォルダも、`parent-strategy` 等と同様にスラッシュコマンド化すると運用が楽になる可能性がある
- 大量の学校情報収集が必要な場面（複数校の過去問傾向を一気に比較する等）では `general-purpose` エージェントの活用を検討する余地がある
